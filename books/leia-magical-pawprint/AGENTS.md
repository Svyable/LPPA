# Book 1 Agent Instructions

Scope: `books/leia-magical-pawprint/`.

The immediate critical path is:

1. review the 19-scene plan;
2. map existing Leia art to scene keepers/gaps;
3. establish the definitive Leia consistency sheet;
4. rebuild only keeper art at genuine print quality;
5. simplify line art for ages roughly 4–8;
6. finish front/back matter;
7. assemble and QA the exact 42-page interior;
8. build the exact KDP wrap cover;
9. finalize listing metadata;
10. upload/preview, then stop before ordering a proof unless a human approves.

Do not bypass the protected decisions in the root `AGENTS.md`.

Whenever a scene or page changes, update both the human-readable Markdown and the matching JSON manifest. Whenever task status changes, update the book tracker in the same PR.
