# New Book Module Template

Use `python scripts/new_book.py <slug> "<title>" "<subtitle>"` to scaffold a future book. Then register it in `books/index.json`.

Every book module owns its content, assets, production, metadata, QA, tracker, and release state. Do not copy Book 1's 42-page assumptions into a future book unless they are intentionally chosen for that book.
